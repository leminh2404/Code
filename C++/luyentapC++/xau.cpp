#include "iostream"
#include "string"
using namespace std;
int main()
{
    string s = "ae3b4dfe";
    for (int i = 0; i < s.length(); i++)
    {
        if (s[i] >= 48 && s[i] < 57)
        {
            int j = int(s[i] - '\0');
            for (int x = 0; x < j; j++)
            
        }
    }
}